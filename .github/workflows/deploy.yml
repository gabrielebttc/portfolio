name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy sulla VPS via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            cd ${{ secrets.VPS_PROJECT_PATH }}
            git pull origin main
            cd portfolio
            docker stop portfolio-site 2>/dev/null && echo "Container fermato" || echo "Container non trovato, skip"
            docker rm portfolio-site 2>/dev/null && echo "Container rimosso" || echo "Niente da rimuovere, skip"
            docker compose up --build -d
            docker image prune -f
